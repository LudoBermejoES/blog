{
  "name": "blog",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "keywords": [
    "astro",
    "astro-theme",
    "blog",
    "portfolio",
    "cyberpunk"
  ],
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "check:workspace-link": "node scripts/check-workspace-link.mjs",
    "check:adapters": "node scripts/check-adapter-contract.mjs && node scripts/sync-adapters.mjs --check",
    "check:fonts": "node scripts/check-font-assets.mjs",
    "check:about-runtime": "npm run build && node scripts/check-about-runtime-config.mjs",
    "check:no-build": "npm run check:workspace-link && npm run check:adapters && npm run check:fonts && astro check",
    "sync-adapters": "node scripts/sync-adapters.mjs",
    "doctor": "npm run check:adapters && npm run check",
    "check": "npm run check:no-build && npm run check:about-runtime",
    "assets:monitor": "node scripts/generate-monitor-assets.mjs",
    "preview": "astro preview",
    "new-post": "anglefeint-new-post",
    "new-page": "anglefeint-new-page",
    "astro": "astro"
  },
  "dependencies": {
    "@anglefeint/astro-theme": "workspace:*",
    "@astrojs/mdx": "^7.0.5",
    "@astrojs/rss": "^4.0.19",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.1.5",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^6.0.3"
  }
}
